import styled from "styled-components";

const Layout = styled.div`
  display: grid;
  grid-template-columns: 1fr;
  grid-template-rows: minmax(2.5rem, 4.5rem) 1fr minmax(4rem, 8rem);
`;

export default Layout;
